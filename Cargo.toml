[package]
name = "greentic-types"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/greentic-ai/greentic-types"
description = "Shared types for Greentic flows, multi-tenant context, and envelopes."
readme = "README.md"
keywords = ["greentic", "multi-tenant", "context", "flow"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
md5 = "0.7"
