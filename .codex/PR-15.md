# PR-15.md (greentic-types) [OPTIONAL but recommended]
# Title: Add ProviderManifest/ProviderIndex shared structs (pure data, not WIT)

## Goal
Provide a shared, serde-friendly data model for:
- ProviderManifest JSON returned by provider-core `describe()`
- ProviderIndex entries stored by store/deployer/runner

## Non-goals
- Do not add domain-specific provider types
- Do not encode provider categories as enums; use strings

## Tasks
- Add `src/provider.rs`:
  - `ProviderManifest { provider_type, capabilities, ops, config_schema_ref?, state_schema_ref? }`
  - `ProviderRuntimeRef { component_ref, export, world }`
  - `ProviderDecl { provider_type, capabilities, ops, config_schema_ref, state_schema_ref?, runtime, docs_ref? }`
  - `ProviderExtensionInline { providers: Vec<ProviderDecl> }`
- Add tests: serde round-trip for these structs
